# Configs

These JSON files are the canonical, public filter configs that map to dropdown items within each category. The frontend and backend both consume them.

Copy or sync them into:

- `frontend/public/config/filters/` for static hosting
- `backend/config/filters/` for the config API
